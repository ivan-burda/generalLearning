//using the withRouter gives access to history, location, match also to components which are not direct nested routed components



import {withRouter} from 'react-router-dom';

//....

export default withRouter(burger);

